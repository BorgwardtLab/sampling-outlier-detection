// copyright: see the header of "qsp.c"
// read CSV data from "filename"
void readCSVFile(double **X, long* n, long* d, char *filename);

// read text from "filename" (used in readCSVFile)
char *readTextFile(char *filename);
